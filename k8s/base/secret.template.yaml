# =============================================================================
# Template Secret -- DO NOT commit real values. Use sealed-secrets, SOPS,
# External Secrets Operator, or `kubectl create secret ...` directly.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: solarease-secrets
  namespace: solarease
  labels:
    app.kubernetes.io/part-of: solarease
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_ME"
  APP_JWT_SECRET: "REPLACE_ME_WITH_STRONG_256_BIT_SECRET"
  JWT_SECRET: "REPLACE_ME_WITH_STRONG_256_BIT_SECRET"
  SPRING_MAIL_USERNAME: "REPLACE_ME"
  SPRING_MAIL_PASSWORD: "REPLACE_ME"
