spring.application.name=dimensioning-service
server.port=8083

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://postgres:5432/solarease_dimensioning
spring.datasource.username=louay
spring.datasource.password=SolarEase2026!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.com.solarease=DEBUG

# Ollama Manual Configuration
ollama.base-url=${OLLAMA_BASE_URL:http://host.docker.internal:11434}
ollama.model=tinyllama
