# API Configuration
VITE_API_URL=http://localhost:8080

# Optional: direct WebSocket URL (wss://) when backend has TLS.
# If unset, SockJS uses same-origin /ws via Vercel rewrites (HTTP polling only).
# VITE_WS_URL=
